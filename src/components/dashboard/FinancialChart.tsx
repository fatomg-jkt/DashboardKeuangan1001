import { ResponsiveContainer, LineChart, Line, CartesianGrid, XAxis, YAxis, Tooltip, BarChart, Bar, Legend, ComposedChart } from 'recharts';
import { monthlyFinancialSummary } from '../../lib/data/financialDummyData';
import { formatCurrency, formatPercent } from '../../lib/utils/formatCurrency';

export function FinancialChart() {
  return <div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold">Tren Pendapatan, HPP & Laba Kotor</h2><div className="mt-4 h-80"><ResponsiveContainer><ComposedChart data={monthlyFinancialSummary}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={(v)=>formatCurrency(v,true)}/><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Legend/><Bar dataKey="revenue" name="Revenue" fill="#2563eb"/><Bar dataKey="cogs" name="HPP" fill="#f97316"/><Line dataKey="grossProfit" name="Gross Profit" stroke="#10b981" strokeWidth={3}/></ComposedChart></ResponsiveContainer></div></section><section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold">Gross & Net Profit Margin</h2><div className="mt-4 h-80"><ResponsiveContainer><LineChart data={monthlyFinancialSummary}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={(v)=>formatPercent(v)}/><Tooltip formatter={(v:number)=>formatPercent(v)}/><Legend/><Line dataKey="grossMargin" name="Gross Margin" stroke="#0f766e" strokeWidth={3}/><Line dataKey="netProfitMargin" name="Net Margin" stroke="#7c3aed" strokeWidth={3}/></LineChart></ResponsiveContainer></div></section></div>;
}

import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis, Legend } from 'recharts';
import { departmentExpenses, totalDepartmentExpense } from '../../lib/data/financialDummyData';
import { formatCurrency, formatPercent } from '../../lib/utils/formatCurrency';
const colors=['#2563eb','#f97316','#10b981','#8b5cf6','#06b6d4','#64748b'];
export function ExpenseByDepartment(){const data=departmentExpenses.map(d=>({...d,contribution:d.actual/totalDepartmentExpense}));return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">Expense by Department</h2><div className="mt-5 grid gap-6 xl:grid-cols-2"><div className="h-72"><ResponsiveContainer><PieChart><Pie data={data} dataKey="actual" nameKey="department" outerRadius={95} label>{data.map((_,i)=><Cell key={i} fill={colors[i%colors.length]}/>)}</Pie><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Legend/></PieChart></ResponsiveContainer></div><div className="h-72"><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="department" hide/><YAxis tickFormatter={(v)=>formatCurrency(v,true)}/><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Legend/><Bar dataKey="actual" fill="#2563eb"/><Bar dataKey="budget" fill="#cbd5e1"/></BarChart></ResponsiveContainer></div></div><div className="mt-4 grid gap-2 md:grid-cols-2 lg:grid-cols-3">{data.map(d=><div key={d.department} className="rounded-xl bg-slate-50 p-3 text-sm"><b>{d.department}</b><p>{formatCurrency(d.actual)} • {formatPercent(d.contribution)}</p></div>)}</div></section>}

import { currentMonth } from '../../lib/data/financialDummyData';
import { formatCurrency, formatPercent } from '../../lib/utils/formatCurrency';
const rows = [['Revenue / Pendapatan', currentMonth.revenue], ['Cost of Goods Sold / HPP', -currentMonth.cogs], ['Gross Profit / Laba Kotor', currentMonth.grossProfit], ['Operating Expenses', -currentMonth.operatingExpenses], ['Operating Profit', currentMonth.operatingProfit], ['Other Income / Expense', currentMonth.otherIncome - currentMonth.otherExpenses], ['Net Profit / Laba Bersih', currentMonth.netProfit]] as const;
export function ProfitLossTable(){return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">Profit & Loss Statement</h2><div className="mt-4 overflow-x-auto table-scroll"><table className="w-full min-w-[620px] text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="py-3">Item</th><th>Amount</th><th>Margin to Revenue</th></tr></thead><tbody>{rows.map(([label, amount])=><tr key={label} className="border-b last:border-0"><td className="py-3 font-medium">{label}</td><td className={amount<0?'text-rose-600':'text-slate-900'}>{formatCurrency(amount)}</td><td>{formatPercent(amount/currentMonth.revenue)}</td></tr>)}</tbody></table></div></section>}

import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { topProducts } from '../../lib/data/financialDummyData';
import { formatCurrency, formatNumber, formatPercent } from '../../lib/utils/formatCurrency';
export function TopProducts(){return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">Top 5 Product Sales</h2><div className="mt-5 grid gap-6 xl:grid-cols-2"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-sm"><thead><tr className="border-b text-left text-slate-500"><th>#</th><th>Produk</th><th>Sales</th><th>Qty</th><th>Gross Profit</th><th>GM</th><th>Contribution</th></tr></thead><tbody>{topProducts.map((p,i)=><tr className="border-b" key={p.name}><td className="py-3 font-bold">{i+1}</td><td>{p.name}</td><td>{formatCurrency(p.sales,true)}</td><td>{formatNumber(p.quantity)}</td><td>{formatCurrency(p.grossProfit,true)}</td><td>{formatPercent(p.grossMargin)}</td><td>{formatPercent(p.contribution)}</td></tr>)}</tbody></table></div><div className="h-72"><ResponsiveContainer><BarChart data={topProducts} layout="vertical"><CartesianGrid strokeDasharray="3 3"/><XAxis type="number" tickFormatter={(v)=>formatCurrency(v,true)}/><YAxis type="category" dataKey="name" width={130}/><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Bar dataKey="sales" fill="#2563eb"/></BarChart></ResponsiveContainer></div></div></section>}

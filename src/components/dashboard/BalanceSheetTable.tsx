import { balanceSheet, totalAssets, totalEquity, totalLiabilities, validationDifference } from '../../lib/data/financialDummyData';
import { formatCurrency } from '../../lib/utils/formatCurrency';
function Group({title,items}:{title:string;items:{name:string;amount:number}[]}){return <div><h3 className="font-semibold text-slate-700">{title}</h3>{items.map(i=><div className="flex justify-between border-b py-2 text-sm" key={i.name}><span>{i.name}</span><span className="font-medium">{formatCurrency(i.amount)}</span></div>)}</div>}
export function BalanceSheetTable(){const valid=validationDifference===0;return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-lg font-bold">Balance Sheet</h2><span className={`rounded-full px-3 py-1 text-xs font-bold ${valid?'bg-emerald-50 text-emerald-700':'bg-rose-50 text-rose-700'}`}>{valid?'Balanced':'Selisih '+formatCurrency(validationDifference)}</span></div><div className="mt-5 grid gap-6 lg:grid-cols-3"><Group title="Assets" items={balanceSheet.assets}/><Group title="Liabilities" items={balanceSheet.liabilities}/><Group title="Equity" items={balanceSheet.equity}/></div><div className="mt-5 grid gap-3 md:grid-cols-3"><b>Total Assets: {formatCurrency(totalAssets)}</b><b>Total Liabilities: {formatCurrency(totalLiabilities)}</b><b>Total Equity: {formatCurrency(totalEquity)}</b></div></section>}

import { Area, AreaChart, Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { cashflow, endingCashBalance, monthlyFinancialSummary, netCashflow } from '../../lib/data/financialDummyData';
import { formatCurrency } from '../../lib/utils/formatCurrency';
export function CashflowChart(){return <section className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold">Cashflow / Arus Kas</h2><div className="mt-4 grid gap-4 md:grid-cols-3"><b>Operating: {formatCurrency(cashflow.operating)}</b><b>Net Cashflow: {formatCurrency(netCashflow)}</b><b>Ending Cash: {formatCurrency(endingCashBalance)}</b></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><div className="h-72"><ResponsiveContainer><BarChart data={monthlyFinancialSummary}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={(v)=>formatCurrency(v,true)}/><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Legend/><Bar dataKey="cashInflow" name="Cash Inflow" fill="#16a34a"/><Bar dataKey="cashOutflow" name="Cash Outflow" fill="#dc2626"/></BarChart></ResponsiveContainer></div><div className="h-72"><ResponsiveContainer><AreaChart data={monthlyFinancialSummary}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={(v)=>formatCurrency(v,true)}/><Tooltip formatter={(v:number)=>formatCurrency(v)}/><Area dataKey="endingCash" name="Ending Cash Balance" fill="#dbeafe" stroke="#2563eb"/></AreaChart></ResponsiveContainer></div></div></section>}

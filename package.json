{
  "name": "DashboardKeuangan1001",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "node --check src/main.js && node --check src/App.js && rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "dependencies": {},
  "devDependencies": {}
}

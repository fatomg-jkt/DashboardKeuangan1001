{
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "node --check src/main.js && rm -rf public && mkdir -p public && cp index.html public/index.html && cp -R src public/src",
    "preview": "python3 -m http.server 4173 --directory public"
  },
  "dependencies": {},
  "devDependencies": {}
}
